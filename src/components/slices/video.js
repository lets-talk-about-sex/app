import React from 'react';
// import { get } from 'lodash';
// import { RichText } from 'prismic-reactjs';
import { graphql } from 'gatsby';
// import styled from '@emotion/styled/macro';

const SliceVideo = ({video}) => {
  
    return (
        <div>   
            <img src={video}></img>
        </div>
    )};
    

export default SliceVideo;